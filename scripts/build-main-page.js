import fs from 'fs'
import path from 'path'

const mainPageContent = `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tapout Testing</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            min-height: 100vh;
            margin: 1rem;
        }

        h2 {
            border-bottom: 2px solid;
        }
        
        .header p {
            font-size: 1.2rem;
        }
        
        .btn {
            border: 2px solid;
            color: black;
            display: inline-block;
            padding: 0.75rem 1.5rem;
            text-decoration: none;
            font-weight: 500;
        }
        
        .btn:hover {
            transform: translateY(-2px);
        }
        
        .footer {
            text-align: center;
            margin-bottom: 2rem;
            width: calc(100% - 2rem);
            position: fixed;
            bottom: 0;
            text-align: center;
        }
        
        .footer a {
            text-decoration: underline;
        }
        
        @media (max-width: 768px) {
            .header h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="header">
            <h1> Tapout</h1>
            <p>Run tests in a browser from the command line</p>
        </header>
        
        <h2>Example Test Results</h2>
        <p>
            An example of the HTML generated by <code>tapout</code>
        </p>
        <a href="./examples/simple-test-results.html" class="btn">View Example</a>
    
        <h2>API Docs</h2>

        <a href="./api/" class="btn">View Documentation</a>
        
        <div class="footer">
            <p>
                Made with <span aria-label="love" role="img">❤️</span> by
                <a
                    href="https://github.com/substrate-system/tapout"
                    target="_blank"
                >Substrate System</a></p>
        </div>
    </div>
</body>
</html>`

// Ensure the docs directory exists
const docsDir = path.join(process.cwd(), 'docs')
if (!fs.existsSync(docsDir)) {
    fs.mkdirSync(docsDir, { recursive: true })
}

// Write the main page
fs.writeFileSync(path.join(docsDir, 'index.html'), mainPageContent)
console.log('Main GitHub Pages index.html created successfully')
